const SORT_OPTIONS = {
  'Default': 'Default',
  'Risk Rating low to high': 'Risk Rating low to high',
  'Risk Rating high to low': 'Risk Rating high to low',

}

const FILTER_OPTIONS = {
  
}

const ROWS_PER_PAGE = 10;

const DEFAULT_MAP_ZOOM = 5;

export { 
  SORT_OPTIONS, 
  FILTER_OPTIONS, 
  ROWS_PER_PAGE,
  DEFAULT_MAP_ZOOM, 
};